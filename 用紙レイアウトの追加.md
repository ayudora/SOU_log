# 用紙レイアウトの追加

`label-core.js` 冒頭の `PAPER_PROFILES` に定義を追加します。HTML・CSS・app.jsの面数変更は不要です。用紙サイズはA4のまま、同一サイズのマスを複数配置する設計です。

例えば、既存の `"a4-business-10": Object.freeze({...}),` の後、`PAPER_PROFILES` を閉じる `});` の前に次を追加します。

```javascript
  "a4-square-20": Object.freeze({
    id: "a4-square-20", // オブジェクトのキーと一致させる
    name: "A4 自作シール 20面（50×50mm）",
    note: "4列×5段／自作用紙",
    sheetWidth: 210,
    sheetHeight: 297,
    width: 50,
    height: 50,
    columns: 4,
    rows: 5,
    left: 5,
    top: 23.5,
    gapX: 0,
    gapY: 0,
  }),
```

`width`・`height` は1片の横・縦、`left`・`top` は最初のマスの左・上余白、`gapX`・`gapY` は隙間です。すべてmm単位。86.4×50.8mm等を追加する際も、その商品の実際の余白と隙間を確認して定義してください。面数だけでは位置が決まりません。

## 不規則な配置

同一サイズのマスを任意位置に置く場合は `positions` を追加します。

```javascript
positions: [
  { left: 10, top: 20 },
  { left: 110, top: 20 },
  { left: 10, top: 100 },
],
```

`positions` がある場合は、その要素数が面数になり、配列順が番号になります。`columns`・`rows`・`left`・`top`・`gapX`・`gapY` の代わりに各座標を使います。倍率補正の原点は最初のマスです。各要素には左上座標だけを指定し、1片の大きさは定義共通のwidth・heightを使います。

## 用紙変更時の動作

- 新しい定義は自動で用紙選択に表示されます。
- 変更前に確認画面を表示し、キャンセルすれば現在の用紙を維持します。
- 変更を確定するとログ・下書きを保持し、配置・使用済み指定・印刷補正をリセットします。
- 保存した写真メモの画像を新サイズで作り直します。元のテキストと写真は保持します。
- 小さいマスで文字が収まらない場合は編集が必要と表示し、印刷させません。
- 保存データの検証は定義された面数に追従します。保存に使われている用紙IDは削除・変更しないでください。

初期版に追加規格は登録していません。必要なときに定義を追加し、横向き・縦向き・長文のプレビューと普通紙テスト印刷を確認してください。異なるサイズのマスを1ページ内に混在させる用途は別途拡張が必要です。

## 検証

`node check-update.cjs` は、配布用定義を書き換えずに検証用20面を読み込み、切り替え・キャンセル・保存データの面数チェック・任意配置の境界チェックを行います。
